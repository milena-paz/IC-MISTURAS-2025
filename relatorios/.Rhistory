cont<-cont+1
}
return(cbind(p,mu,sig))
}
#chute inicial de equiprobabilidade
EM(c(0.5,0.5),c(160,160),c(54,54))
y
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-10
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
EM(c(0.2,0.8),c(160,160),c(54,54))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
EM(c(0.2,0.8),c(160,160),c(54,54))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
EM(c(0.2,0.8),c(160,170),c(54,54))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
EM(c(0.2,0.8),c(160,170),c(10,20))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
EM(c(0.2,0.8),c(120,150),c(10,20))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
EM(c(0.5,0.5),c(140,150),c(10,20))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
#chute baseado no grafico
EM(c(0.3,0.7),c(175,158),c(10,10))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
#chute baseado no histograma
EM(c(0.3,0.7),c(175,158),c(10,10))
#| fig-align: center
#Fixando os parâmetros
P <- c(0.64,0.36)
mu <- c(160,173)
sigma <- sqrt(c(15,12))
n<- 30
grupos <- t(rmultinom(n, 1, prob=P))
y <- numeric(n)
for(i in 1:2)
y[as.logical(grupos[,i])] <- rnorm(sum(grupos[,i]), mu[i],sigma[i])
hist(y,freq=F,breaks=20, main="Histograma da mistura gerada")
curve(P[1]*dnorm(x,mu[1],sigma[1]) + P[2]*dnorm(x,mu[2],sigma[2]),add=T,col="blue",lwd=2)
#| fig-align: center
#Fixando os parâmetros
P <- c(0.64,0.36)
mu <- c(160,173)
sigma <- sqrt(c(15,12))
n<- 100
grupos <- t(rmultinom(n, 1, prob=P))
y <- numeric(n)
for(i in 1:2)
y[as.logical(grupos[,i])] <- rnorm(sum(grupos[,i]), mu[i],sigma[i])
hist(y,freq=F,breaks=20, main="Histograma da mistura gerada")
curve(P[1]*dnorm(x,mu[1],sigma[1]) + P[2]*dnorm(x,mu[2],sigma[2]),add=T,col="blue",lwd=2)
l <- function(p){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p){
ll2 <- l(p)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-7
}
component <- function(x,g){
dnorm(x,mu[g],sigma[g])
}
EM <- function(p){
cont<-1
l1<-100
z <- 1
while(crit(l1,p)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,j)/sum(p[1]*component(y,1),p[2]*component(y,2)))
})
#-----------------------#
l1<- l(p)
#--------PASSO M--------#
p <- apply(z,1,mean)
#-----------------------#
cat("Iteração ",cont,": phat=",p,"\n")
cont<-cont+1
}
return(p)
}
#chute inicial de equiprobabilidade
EM(c(0.5,0.5))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
#chute baseado no histograma
EM(c(0.3,0.7),c(175,158),c(10,10))
EM(c(0.5,0.5),c(160,160),c(54,54))
EM(c(0.9,0.1),c(160,160),c(54,54))
EM(c(0.9,0.1),c(160,160),c(2,2))
EM(c(0.9,0.1),c(160,164),c(2,2))
#| fig-align: center
#Fixando os parâmetros
P <- c(0.64,0.36)
mu <- c(160,173)
sigma <- sqrt(c(15,12))
n<- 500
grupos <- t(rmultinom(n, 1, prob=P))
y <- numeric(n)
for(i in 1:2)
y[as.logical(grupos[,i])] <- rnorm(sum(grupos[,i]), mu[i],sigma[i])
hist(y,freq=F,breaks=20, main="Histograma da mistura gerada")
curve(P[1]*dnorm(x,mu[1],sigma[1]) + P[2]*dnorm(x,mu[2],sigma[2]),add=T,col="blue",lwd=2)
l <- function(p){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p){
ll2 <- l(p)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-7
}
component <- function(x,g){
dnorm(x,mu[g],sigma[g])
}
EM <- function(p){
cont<-1
l1<-100
z <- 1
while(crit(l1,p)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,j)/sum(p[1]*component(y,1),p[2]*component(y,2)))
})
#-----------------------#
l1<- l(p)
#--------PASSO M--------#
p <- apply(z,1,mean)
#-----------------------#
cat("Iteração ",cont,": phat=",p,"\n")
cont<-cont+1
}
return(p)
}
#chute inicial de equiprobabilidade
EM(c(0.5,0.5))
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
#chute baseado no histograma
EM(c(0.3,0.7),c(175,158),c(10,10))
#| fig-align: center
#Fixando os parâmetros
P <- c(0.64,0.36)
mu <- c(160,173)
sigma <- sqrt(c(15,12))
n<- 500
grupos <- t(rmultinom(n, 1, prob=P))
y <- numeric(n)
for(i in 1:2)
y[as.logical(grupos[,i])] <- rnorm(sum(grupos[,i]), mu[i],sigma[i])
hist(y,freq=F,breaks=20, main="Histograma da mistura gerada")
curve(P[1]*dnorm(x,mu[1],sigma[1]) + P[2]*dnorm(x,mu[2],sigma[2]),add=T,col="blue",lwd=2)
#usando os mesmos dados do exemplo 2.1
l <- function(p,mu,sigma){
sum(log(p[1]*dnorm(y,mu[1],sigma[1])+p[2]*dnorm(y,mu[2],sigma[2])))
}
crit <- function(ll,p,mu,sig){
ll2 <- l(p,mu,sig)
abs(ll2-ll)/(abs(ll) + 1e-8) > 1e-6
}
component <- function(x,mu,sig,g){
dnorm(x,mu[g],sqrt(sig[g]))
}
EM <- function(p,mu,sig){
cont<-1
l1<-100
z <- 1
while(crit(l1,p,mu,sig)){
#--------PASSO E--------#
z <- sapply(y,function(y){
sapply(1:2,function(j) p[j]*component(y,mu,sig,j)/sum(p[1]*component(y,mu,sig,1),
p[2]*component(y,mu,sig,2)))
})
#-----------------------#
l1<- l(p,mu,sig)
#--------PASSO M--------#
p <- apply(z,1,mean)
mu <- apply(z,1,function(z) sum(z*y)/sum(z))
sig <- sapply(1:2, function(j)
sum(z[j,]*(y-mu[j])^2)/sum(z[j,]))
#-----------------------#
cat("Iteração ",cont,": phat=",p," muhat= ", mu,"sigma2hat", sig,"\n")
cont<-cont+1
}
return(cbind(p,mu,sig))
}
#chute baseado no histograma
EM(c(0.3,0.7),c(175,158),c(10,10))
sum(0.3850786,0.6149214 )
